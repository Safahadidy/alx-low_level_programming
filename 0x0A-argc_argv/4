#include <stdio.h>
#include <stdlib.h>

/**
* description: main - Program That Prints It's Name
* @argc: The Args Count
* @argv: The Array Or Args
* Return: 0
*/
#include <stdio.h>
int main(int argc, char *argv[])
{  
int num1, int num2;
if (argv != 3)
{
return (1);
printf("Error\n");
}
num1 = atoi(argv[1]);
num2 = atoi(argv[1]);
printf("%d%d\n", num1 * num2 );
return (0);
}

